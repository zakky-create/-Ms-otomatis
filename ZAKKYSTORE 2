local player = game.Players.LocalPlayer
local TweenService = game:GetService("TweenService")

-- koordinat tujuan
local npc = Vector3.new(523,3,615)
local apt1 = Vector3.new(1202,3,-182)

-- cari kursi kendaraan
local function getSeat()

    local char = player.Character
    if not char then return end

    local humanoid = char:FindFirstChildOfClass("Humanoid")

    if humanoid and humanoid.SeatPart then
        return humanoid.SeatPart
    end

end

-- teleport kendaraan
local function teleport(pos)

    local seat = getSeat()

    if seat then

        local vehicle = seat.Parent

        for _,part in pairs(vehicle:GetDescendants()) do
            if part:IsA("BasePart") then

                local tween = TweenService:Create(
                    part,
                    TweenInfo.new(2, Enum.EasingStyle.Linear),
                    {CFrame = part.CFrame + (pos - seat.Position)}
                )

                tween:Play()

            end
        end

    end

end

-- GUI ZAKKYSTORE
local gui = Instance.new("ScreenGui", player.PlayerGui)

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0,220,0,300)
frame.Position = UDim2.new(0.02,0,0.3,0)
frame.BackgroundColor3 = Color3.fromRGB(30,30,30)
frame.Active = true
frame.Draggable = true
frame.Parent = gui

local title = Instance.new("TextLabel")
title.Parent = frame
title.Size = UDim2.new(1,0,0,30)
title.Text = "ZAKKYSTORE"
title.TextColor3 = Color3.new(1,1,1)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.TextSize = 16

-- tombol teleport NPC
local btnNPC = Instance.new("TextButton")
btnNPC.Size = UDim2.new(1,0,0,40)
btnNPC.Position = UDim2.new(0,0,0,60)
btnNPC.Text = "TELEPORT NPC"
btnNPC.Parent = frame

-- tombol teleport APT 1
local btnAPT = Instance.new("TextButton")
btnAPT.Size = UDim2.new(1,0,0,40)
btnAPT.Position = UDim2.new(0,0,0,110)
btnAPT.Text = "TELEPORT APT 1"
btnAPT.Parent = frame

btnNPC.MouseButton1Click:Connect(function()
    teleport(npc)
end)

btnAPT.MouseButton1Click:Connect(function()
    teleport(apt1)
end)

